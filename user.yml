- name: set up user server
  hosts: user
  become: yes
  tasks:
  - name: disable nodejs
    ansible.builtin.command: dnf module disable nodejs -y

  - name: enabled nodejs20
    ansible.builtin.command: dnf module enable nodejs:20 -y

  - name: install node js
    ansible.builtin.package:
      name: nodejs 
      state: present

  - name: addinng roboshop user
    ansible.builtin.user:
      name: roboshop
      comment: roboshop user
      shell: /sbin/nologin
      home: /app
      system: true
  

   # removing the directory , if we run 2 nd time it will doenload freshly.
  - name: removing the app directory
    ansible.builtin.file:
      path: /app
      state: absent

  - name: creating the app directory
    ansible.builtin.file:
      path: /app
      state: directory

  
  - name: removing the userapplication
    ansible.builtin.file:
      path: /tmp/user.zip
      state: absent


  - name: downloading the user application
    ansible.builtin.get_url:
      url: https://roboshop-artifacts.s3.amazonaws.com/user-v3.zip 
      dest: /tmp/user.zip

  - name: unzip the user file
    ansible.builtin.unarchive:
      src: /tmp/user.zip
      dest: /app

  - name: installing dependencies
    community.general.npm:
      path: /app

  - name: copy user service file
    ansible.builtin.copy:
      src: user.service
      dest: /etc/systemd/system/user.service

  - name: daemon reload and start services
    ansible.builtin.systemd_service:
      state: started
      name: user 
      enabled: yes
      daemon_reload: true